-- Transpilado por Zenith v0.2.5
local zt = require("src.backend.lua.runtime.zenith_rt")
local Present = zt.Optional.Present
local Empty = zt.Optional.Empty
local Success = zt.Outcome.Success
local Failure = zt.Outcome.Failure

local is_even

function is_even(_p1)
    function bang(_p1)
        (zt.add("Bang! ", n))
        nil
        local User = {}
        User.__index = User
        User._metadata = {
            name = "User",
            fields = {
            },
            methods = {
            }
        }
        
        function User.new(fields)
            local self = setmetatable({}, User)
            self. = fields. or nil
            self.name = fields.name or nil
            self.var = fields.var or nil
            self. = fields. or nil
            return self
        end
    end
end

-- Struct Methods

return {
}