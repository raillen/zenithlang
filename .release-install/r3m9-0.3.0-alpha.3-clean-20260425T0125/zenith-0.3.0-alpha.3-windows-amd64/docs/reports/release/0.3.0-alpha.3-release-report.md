# Zenith 0.3.0-alpha.3 Release Report

Status: finalized with local verifiable artifacts
Date: 2026-04-24
Owner: R3.M9

## Decision

R3 is finalized as `0.3.0-alpha.3`.

The package, checksum, clean-install log, compatibility report and release notes are present in the repository.

This report does not claim that a GitHub Release asset was uploaded.

## Gates

| Gate | Result | Evidence |
|---|---|---|
| `python build.py` | pass | compiler rebuild completed |
| `python run_all_tests.py` | pass | `186` pass, `0` fail, `2` skip |
| `tests/perf/gate_pr.ps1` | pass | `167/167` |
| `tests/perf/gate_nightly.ps1` | pass with override | `docs/governance/perf-overrides/R3.M9-release-baseline-acceptance.md` |
| clean install | pass | `docs/reports/release/artifacts/zenith-0.3.0-alpha.3-clean-install.log` |

## Fixed During Finalization

- `behavior/borealis_backend_fallback_stub` now uses the deterministic stub backend.
- `hardening/differential_validate_between` passes in PR gate.
- PR perf gate passes through the documented R3.M9 override.
- Nightly perf gate exits successfully through the documented R3.M9 override.

## Artifact Evidence

Metadata:

- `docs/reports/release/artifacts/R3.M9-release-artifacts-0.3.0-alpha.3.json`

Package:

- `docs/reports/release/artifacts/zenith-0.3.0-alpha.3-windows-amd64.zip`

Checksum:

- `docs/reports/release/artifacts/zenith-0.3.0-alpha.3-windows-amd64.checksums.txt`

Clean install:

- `docs/reports/release/artifacts/zenith-0.3.0-alpha.3-clean-install.log`

## Residual Risk

R3 is acceptable for alpha release.

Known residual risk:

- perf baseline drift is accepted for R3.M9 and must be refreshed in R4;
- full concurrency runtime surface is not part of R3 and is moved to `R4.CF1`;
- FFI callbacks remain future work;
- `lazy<T>` generic runtime remains future work.

## Release Conclusion

R3.M9 acceptance is satisfied for a local source/artifact release:

- artifacts are verifiable;
- install is validated;
- no P0 remains open without acceptance;
- performance drift has explicit acceptance.
