# Packages Workspace

This folder holds local package prototypes.

Goal:

- keep package work outside `stdlib`;
- make migration to ZPM direct when `zpm install` lands.

Current packages:

- `borealis` (Borealis) - game framework with 2D runtime/editor foundations and an experimental Raylib-backed 3D slice.
