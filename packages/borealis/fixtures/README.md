# Borealis Fixtures

Fixtures are integration samples.

They may be longer than examples because their job is to exercise several
modules together.

Use `packages/borealis/examples` for onboarding.
Use this folder for broad package validation and contract reference.

Current fixtures:

- `modular_scaffolds_v1.zt`: validates the scaffold modules as one integration
  flow.
