namespace app.main

struct Player
    hp: int = 100
    bonus: int = 17
    name: text = Ada
end

func main() -> int 
    const player: Player = Player()
    if player.hp + player.bonus > 0 
        return 0
    end
    return 1
end
