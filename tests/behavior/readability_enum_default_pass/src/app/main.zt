namespace app.main

enum Mode
    Fast
    Safe
end

func score(mode: Mode) -> int
    match mode
        case Mode.Fast ->
            return 1
        case default ->
            return 2
    end
end

func main() -> int
    const mode: Mode = Mode.Fast
    return score(mode) - 1
end
