namespace app.main

enum Shape
    Circle(radius: int)
    Rectangle(width: int, height: int)
end

func get_area(s: Shape) -> int
    match s
        case Shape.Circle(r) ->
            return r * r
    end
end

func main() -> int
    return 0
end
