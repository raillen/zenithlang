namespace app.main

func greet(name: text where name.length > 0 and name.length + undefined_var < 100) -> text
    return "Hello, " + name
end

func main() -> int
    greet("Zenith")
    return 0
end
