# R2.M10 - Registro de Compatibilidade

Data: 2026-04-21

## Mudanças de API

Mudanças aditivas (sem quebra):

- `std.validate.one_of_text(value: text, candidates: list<text>) -> bool`

## Fluxo oficial de qualidade

Mudança de processo (sem quebra de linguagem):

- `run_all_tests.py` agora executa gate oficial de tooling:
  - `zt fmt --check` em projeto canário
  - `zt doc check` em projeto canário

## Risco residual

- `zt fmt --check .` no repositório completo ainda pode falhar em arquivos legados fora do projeto canário.
- `zt doc check .` global ainda encontra erros antigos em zdoc legado.
