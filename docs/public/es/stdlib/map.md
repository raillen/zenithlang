# Módulo`std.map`

Pequeños ayudantes para empotrar.`map<K,V>`valores.

Ayudantes conocidos por el compilador:

- `map.is_empty(values)`regresa`true`cuando el mapa no tiene entradas.
-`map.has_key(values, key)`regresa`true`cuando el mapa contiene la clave.

## Constantes y funciones

