# R4.M0 - Alpha.2 Stabilization Base Report

**Date:** 2026-04-24  
**Status:** Concluido  
**Author:** AI Assistant

## Resumo

Milestone R4.M0 focada em estabilizacao da base de codigo para alpha.2. Principal conquista: **zero warnings de build** no compilador C, melhoria significativa na qualidade dos testes (168/172 pass vs 112/119 anterior).

## Entregas

### 1. Warnings de build - Zero warnings

**Antes:** 5 warnings  
**Depois:** 0 warnings

Correcoes aplicadas:
- `checker.c:806` - `zt_checker_validate_dyn_trait` unused function → wired into type resolution for `dyn<Trait>` types
- `emitter.c:10022` - unused parameters `emitter` and `result` in `c_emit_vtable_type_definitions` → added `(void)` casts
- `emitter.c:1018,1049` - format-truncation warnings → added precision specifiers `%.115s` and `%.122s` in `snprintf`

### 2. Baselines atualizadas

| Metrica | R2.M0 (anterior) | R4.M0 (atual) | Delta |
|---------|-------------------|----------------|-------|
| Total testes | 119 | 172 | +53 |
| Pass | 112 | 168 | +56 |
| Fail | 6 | 2 | -4 |
| Skip | 1 | 2 | +1 |
| Warnings build | 5 | 0 | -5 |

### 3. R3.M4 dyn dispatch - Correcoes integradas

Fixes aplicados durante esta milestone que impactam estabilidade:
- `<stdlib.h>` include no emitter prologue para `malloc()`
- Syntax bug em `ZIR_EXPR_CALL_DYN` emission (parenteses mismatch)
- Vtable struct layout mismatch → uso direto de `zt_vtable` com `methods[]`
- `zt_retain` em vtable const → removido (vtables sao static const)
- Forward declarations para `c_to_lower`, `c_extract_method_name`, `c_find_trait_method_index`

## Falhas conhecidas (nao bloqueantes)

| Teste | Causa | Severidade |
|-------|-------|------------|
| `list_dyn_textrepresentable` | Bug pre-existente: string literal nao eh boxed para `zt_dyn_text_repr` quando atribuido a `dyn<TextRepresentable>` | P1 |
| `test_m9.exe` | Golden mismatch 33/34 no conformance test | P2 |

## Baselines

- `docs/governance/baselines/quality-baseline.json` - atualizado
- `docs/governance/baselines/perf-baseline.json` - mantido (validar com gate_nightly)

## Proximos passos (R4.M1+)

1. Corrigir boxing de string literal para `dyn<TextRepresentable>` (P1)
2. Investigar golden mismatch no test_m9 (P2)
3. R4.M1: LSP v1 + VSCode beta
